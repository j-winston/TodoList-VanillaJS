// domCoordinator.js
// Role: Coordinates all DOM related activity
// Responsibilities: Add/remove/listen to DOM elements. Broadcast DOM events to pubSub

const domCoordinator = (() => {
  const _publishEvent = () => {};

  const listenTo = (events) => {};
  const displayEl = (el) => {};

  return {
    listenTo,
    displayEl,
  };
})();
